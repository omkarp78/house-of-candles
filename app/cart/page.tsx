"use client";import{useCart}from"@/components/Cart";import{useState}from"react";export default function Cart(){const{items,qty}=useCart();const[name,setName]=useState(""),[phone,setPhone]=useState(""),[address,setAddress]=useState(""),[city,setCity]=useState(""),[pin,setPin]=useState("");const total=items.reduce((a:any,i:any)=>a+i.p.price*i.q,0);function order(){if(!name||!phone||!address||!city||!pin)return alert("Please fill all delivery details.");const lines=items.map((i:any)=>`• ${i.p.name} x${i.q} — ₹${i.p.price*i.q}`).join("\n");const msg=`Hi House of Candles, I would like to place an order:\n\n${lines}\n\nTotal: ₹${total}\n\nName: ${name}\nPhone: ${phone}\nAddress: ${address}, ${city} - ${pin}`;location.href="https://wa.me/918830424127?text="+encodeURIComponent(msg)}return <main className="section"><h1>Your Bag</h1>{!items.length?<div className="empty">♡<h2>Your bag is waiting for a little glow.</h2></div>:<><div>{items.map((i:any)=><div className="cartrow" key={i.p.id}><b>{i.p.name}</b><span>₹{i.p.price}</span><div><button onClick={()=>qty(i.p.id,i.q-1)}>−</button> {i.q} <button onClick={()=>qty(i.p.id,i.q+1)}>+</button></div></div>)}</div><h2>Subtotal ₹{total}</h2><div className="form"><input placeholder="Customer name" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="Phone" value={phone} onChange={e=>setPhone(e.target.value)}/><input placeholder="Address" value={address} onChange={e=>setAddress(e.target.value)}/><input placeholder="City" value={city} onChange={e=>setCity(e.target.value)}/><input placeholder="Pincode" value={pin} onChange={e=>setPin(e.target.value)}/></div><button className="btn" onClick={order}>Order on WhatsApp</button></>}</main>}