"use client";import{products}from"../../../lib/products";import{useCart}from"../../../components/Cart";import{useParams}from"next/navigation";export default function Product(){const{id}=useParams<{id:string}>();const p=products.find(x=>x.id===id);const{add}=useCart();if(!p)return <main className="section"><h1>Product not found</h1></main>;return <main className="detail"><div className={"art big "+p.id}><span>{p.emoji}</span></div><div><small>{p.status==="coming"?"COMING SOON":"HANDCRAFTED"}</small><h1>{p.name}</h1>{p.status==="available"&&<h2>₹{p.price}</h2>}<p>{p.desc}</p>{p.id==="heart"&&<ul><li>Size: approx 3 cm each</li><li>Weight: approx 20 g each</li><li>Material: Premium wax</li><li>Fragrance: Available in multiple scents</li><li>Perfect for gifts, return gifts & home decor</li></ul>} {p.status==="available"?<button className="btn" onClick={()=>add(p)}>Add to Cart</button>:<a className="btn" href={"https://wa.me/918830424127?text="+encodeURIComponent("Notify me when "+p.name+" is available.")}>Notify Me</a>}</div></main>}