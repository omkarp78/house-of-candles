"use client";import{useState}from"react";import ProductCard from"../../components/ProductCard";import{products}from"../../lib/products";export default function Shop(){const[q,setQ]=useState("");const[c,setC]=useState("All");const cats=["All","Heart","Flower","Gift Sets","Tealights"];const list=products.filter(p=>(c==="All"||p.category===c)&&p.name.toLowerCase().includes(q.toLowerCase()));return <main className="section"><small>HOUSE OF CANDLES</small><h1>Shop the Collection</h1><input className="search" placeholder="Search candles…" value={q} onChange={e=>setQ(e.target.value)}/><div className="chips">{cats.map(x=><button className={c===x?"active":""} onClick={()=>setC(x)} key={x}>{x}</button>)}</div><div className="grid">{list.map(p=><ProductCard key={p.id} p={p}/>)}</div></main>}