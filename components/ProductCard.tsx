"use client";import Link from"next/link";import{Product}from"../lib/products";import{useCart}from"./Cart";export default function ProductCard({p}:{p:Product}){const{add}=useCart();return <article className="card"><Link href={"/product/"+p.id}><div className={"productPhoto "+p.id}>{p.image?<img src={p.image} alt={p.name}/>:<span>{p.emoji}</span>}<i>♡</i></div><h3>{p.name}</h3><p>{p.desc}</p>{p.status==="available"?<strong>₹{p.price}</strong>:<em>Coming Soon</em>}</Link>{p.status==="available"?<button onClick={()=>add(p)}>🛒 Add to Cart</button>:<a className="outline" href={"https://wa.me/918830424127?text="+encodeURIComponent("Hi House of Candles, please notify me when "+p.name+" is available.")}>Notify Me</a>}</article>}